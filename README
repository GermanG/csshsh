This is a set of scripts targeting a general use, since it was originally intended for particular use, initially it may have some hardcoded things that should be configurable or, at least, not defined.
